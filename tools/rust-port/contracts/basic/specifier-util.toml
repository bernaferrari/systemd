schema = 1
module = "specifier_util"
semantic_coverage = "partial"
source_structure = "independent"
deviation_policy = "explicit-only"
excluded_c_symbols = [
    "specifier_printf",
    "specifier_string",
    "specifier_real_path",
    "specifier_id128",
    "efi_loader_get_entries",
    "efi_loader_entry_title_valid",
    "efi_loader_entry_resource_filename_valid",
]

[authority]
c_headers = ["src/shared/specifier.h", "src/shared/efi-loader.h"]
c_sources = ["src/shared/specifier.c", "src/shared/efi-loader.c"]
rust_headers = ["src/basic/rust/specifier_util.h"]
rust_sources = ["src/basic/rust/specifier_util.rs"]

[[surface]]
id = "specifier-escape"
c_symbols = ["specifier_escape"]
rust_symbols = ["rs_specifier_escape"]
coverage = "exact"
abi = "c-exact"
bytes = "borrowed-cstr-opaque"
errors = "nullable"
runtime = "none"
verification = "static-only"

[[surface.output]]
arg = "return"
ownership = "owned-libc"
release = "free"
publication_success = "return"
publication_error = "return"
optional = false

[surface.static_fixture]
file = "tests-extra/test-specifier-efi-rust.c"
meson_target = "test-specifier-efi-rust"
labels = ["specifier-escape"]

[[surface]]
id = "specifier-escape-strv"
c_symbols = ["specifier_escape_strv"]
rust_symbols = ["rs_specifier_escape_strv"]
coverage = "deviation"
abi = "c-exact"
bytes = "borrowed-cstr-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"
deviation_id = "specifier-escape-strv-null-ret-fail-closed"
deviation_rationale = "The C authority asserts a non-NULL ret pointer; the Rust ABI returns -EINVAL for that violated precondition while preserving the C result and publication contract for valid calls."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-28."

[[surface.output]]
arg = "ret"
ownership = "owned-libc"
release = "free"
publication_success = "write"
publication_error = "unchanged"
optional = false

[surface.static_fixture]
file = "tests-extra/test-specifier-efi-rust.c"
meson_target = "test-specifier-efi-rust"
labels = ["specifier-escape-strv"]

[[surface]]
id = "efi-loader-entry-name-validation"
c_symbols = ["efi_loader_entry_name_valid"]
rust_symbols = ["rs_efi_loader_entry_name_valid"]
coverage = "exact"
abi = "c-exact"
bytes = "borrowed-cstr-opaque"
errors = "boolean"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-specifier-efi-rust.c"
meson_target = "test-specifier-efi-rust"
labels = ["efi-loader-entry-name-validation"]

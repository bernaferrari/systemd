schema = 1
module = "image_policy_util"
semantic_coverage = "partial"
source_structure = "independent"
deviation_policy = "explicit-only"
excluded_c_symbols = ["image_policy_copy", "image_policy_ignore_designators"]

[authority]
c_headers = ["src/shared/image-policy.h"]
c_sources = ["src/shared/image-policy.c"]
rust_headers = ["src/basic/rust/image_policy_util.h"]
rust_sources = ["src/basic/rust/image_policy_util.rs"]

[[surface]]
id = "image-policy-flags-extend"
c_symbols = ["partition_policy_flags_extend"]
rust_symbols = ["rs_partition_policy_flags_extend"]
coverage = "exact"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-flags-extend"]

[[surface]]
id = "image-policy-flags-reduce"
c_symbols = ["partition_policy_flags_reduce"]
rust_symbols = ["rs_partition_policy_flags_reduce"]
coverage = "exact"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-flags-reduce"]

[[surface]]
id = "image-policy-flags-from-string"
c_symbols = ["partition_policy_flags_from_string"]
rust_symbols = ["rs_partition_policy_flags_from_string"]
coverage = "deviation"
abi = "c-exact"
bytes = "borrowed-cstr-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"
deviation_id = "image-policy-flags-null-fail-closed"
deviation_rationale = "Valid byte-oriented flag parsing, graceful unknown-token handling, and empty-segment behavior follow C. Rust returns -EINVAL for a malformed NULL C string instead of C's required-pointer assertion."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-flags-from-string"]

[[surface]]
id = "image-policy-flags-to-string"
c_symbols = ["partition_policy_flags_to_string"]
rust_symbols = ["rs_partition_policy_flags_to_string"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "negative-errno"
runtime = "none"
verification = "static-only"
deviation_id = "image-policy-flags-output-null-fail-closed"
deviation_rationale = "For valid output storage the formatter allocates a C-compatible string and publishes only after allocation. Rust returns -EINVAL for a malformed NULL ret pointer rather than C's assertion behavior."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[[surface.output]]
arg = "ret"
ownership = "owned-libc"
release = "free"
publication_success = "write"
publication_error = "unchanged"
optional = false

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-flags-to-string"]

[[surface]]
id = "image-policy-free"
c_symbols = ["image_policy_free"]
rust_symbols = ["rs_image_policy_free"]
coverage = "exact"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "nullable"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-free"]

[[surface]]
id = "image-policy-lookup"
c_symbols = ["image_policy_get", "image_policy_get_exhaustively"]
rust_symbols = ["rs_image_policy_get", "rs_image_policy_get_exhaustively"]
coverage = "exact"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-lookup"]

[[surface]]
id = "image-policy-equal"
c_symbols = ["image_policy_equal"]
rust_symbols = ["rs_image_policy_equal"]
coverage = "exact"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "boolean"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-equal"]

[[surface]]
id = "image-policy-equivalent"
c_symbols = ["image_policy_equivalent"]
rust_symbols = ["rs_image_policy_equivalent"]
coverage = "exact"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-equivalent"]

[[surface]]
id = "image-policy-special-equivalence"
c_symbols = ["image_policy_equiv_ignore", "image_policy_equiv_allow", "image_policy_equiv_deny"]
rust_symbols = ["rs_image_policy_equiv_ignore", "rs_image_policy_equiv_allow", "rs_image_policy_equiv_deny"]
coverage = "exact"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "boolean"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-special-equivalence"]

[[surface]]
id = "image-policy-parse"
c_symbols = ["image_policy_from_string"]
rust_symbols = ["rs_image_policy_from_string"]
coverage = "deviation"
abi = "c-exact"
bytes = "borrowed-cstr-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"
deviation_id = "image-policy-parse-null-input-fail-closed"
deviation_rationale = "Valid byte-oriented policy expressions retain C grammar, allocator ownership, validation-only NULL ret behavior, and success-only publication. A malformed NULL input string returns -EINVAL rather than entering C assertion behavior."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[[surface.output]]
arg = "ret"
ownership = "owned-libc"
release = "free"
publication_success = "write"
publication_error = "unchanged"
optional = true

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-parse"]

[[surface]]
id = "image-policy-format"
c_symbols = ["image_policy_to_string"]
rust_symbols = ["rs_image_policy_to_string"]
coverage = "deviation"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"
deviation_id = "image-policy-format-required-output-fail-closed"
deviation_rationale = "Valid C-layout flexible-array policies format to a libc-owned string with C result count and publication behavior. A malformed NULL ret pointer returns -EINVAL instead of C assertion behavior."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[[surface.output]]
arg = "ret"
ownership = "owned-libc"
release = "free"
publication_success = "write"
publication_error = "unchanged"
optional = false

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-format"]

[[surface]]
id = "image-policy-set-operations"
c_symbols = ["image_policy_intersect", "image_policy_union"]
rust_symbols = ["rs_image_policy_intersect", "rs_image_policy_union"]
coverage = "exact"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"

[[surface.output]]
symbols = ["image_policy_intersect", "image_policy_union"]
arg = "ret"
ownership = "owned-libc"
release = "free"
publication_success = "write"
publication_error = "unchanged"
optional = true

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-set-operations"]

[[surface]]
id = "image-policy-fstype"
c_symbols = ["partition_policy_determine_fstype"]
rust_symbols = ["rs_partition_policy_determine_fstype"]
coverage = "deviation"
abi = "c-exact"
bytes = "ptr-len-opaque"
errors = "negative-errno"
runtime = "none"
verification = "static-only"
deviation_id = "image-policy-fstype-invalid-arguments-fail-closed"
deviation_rationale = "Valid C-layout policies preserve C's unique-fstype 0/1 result, optional encryption output, libc fstype ownership, and success-only publication. Invalid designators or a NULL required ret_fstype return -EINVAL rather than C assertion behavior."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[[surface.output]]
arg = "ret_fstype"
ownership = "owned-libc"
release = "free"
publication_success = "write"
publication_error = "unchanged"
optional = false

[surface.static_fixture]
file = "tests-extra/test-image-policy-rust.c"
meson_target = "test-image-policy-rust"
labels = ["image-policy-fstype"]

schema = 1
module = "at_flags_util"
semantic_coverage = "complete"
source_structure = "independent"
deviation_policy = "explicit-only"

[authority]
c_headers = ["src/basic/fs-util.h"]
# Header-inline authority: repeat the implementation header because the
# contract schema requires a non-empty implementation path list.
c_sources = ["src/basic/fs-util.h"]
rust_headers = ["src/basic/rust/at_flags_util.h"]
rust_sources = ["src/basic/rust/at_flags_util.rs"]

[[surface]]
id = "at-flags-normalize-nofollow"
c_symbols = ["at_flags_normalize_nofollow"]
rust_symbols = ["rs_at_flags_normalize_nofollow"]
coverage = "exact"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-at-flags-rust.c"
meson_target = "test-at-flags-rust"
labels = ["at-flags-normalize-nofollow"]

[[surface]]
id = "at-flags-normalize-follow"
c_symbols = ["at_flags_normalize_follow"]
rust_symbols = ["rs_at_flags_normalize_follow"]
coverage = "exact"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-at-flags-rust.c"
meson_target = "test-at-flags-rust"
labels = ["at-flags-normalize-follow"]

schema = 1
module = "replace_var"
semantic_coverage = "partial"
source_structure = "independent"
deviation_policy = "explicit-only"
excluded_c_symbols = ["get_variable"]

[authority]
c_headers = ["src/basic/replace-var.h"]
c_sources = ["src/basic/replace-var.c"]
rust_headers = ["src/basic/rust/replace_var.h"]
rust_sources = ["src/basic/rust/replace_var.rs"]

[[surface]]
id = "replace-var-byte-substitution"
c_symbols = ["replace_var"]
rust_symbols = ["rs_replace_var"]
coverage = "exact"
abi = "c-exact"
bytes = "borrowed-cstr-opaque"
errors = "nullable"
runtime = "none"
verification = "static-only"

[[surface.output]]
arg = "return"
ownership = "owned-libc"
release = "free"
publication_success = "return"
publication_error = "return"
optional = false

[surface.static_fixture]
file = "tests-extra/test-replace-var-rust.c"
meson_target = "test-replace-var-rust"
labels = [
    "replace-var-byte-substitution",
    "replace-var-token-boundaries",
    "replace-var-callback-ownership-and-order",
    "replace-var-lookup-failure",
]

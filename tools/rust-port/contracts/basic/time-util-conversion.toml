schema = 1
module = "time-util-conversion"
semantic_coverage = "partial"
source_structure = "independent"
deviation_policy = "explicit-only"
excluded_c_symbols = ["now", "now_nsec", "map_clock_usec", "triple_timestamp_by_clock", "parse_time"]

[authority]
c_headers = ["src/basic/time-util.h"]
c_sources = ["src/basic/time-util.c"]
rust_headers = ["src/basic/rust/time_util.h"]
rust_sources = ["src/basic/rust/time_util/conversion.rs", "src/basic/rust/time_util/types.rs"]

[[surface]]
id = "time-map-clock-usec-raw"
c_symbols = ["map_clock_usec_raw"]
rust_symbols = ["rs_map_clock_usec_raw"]
coverage = "exact"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-map-clock-usec-raw"]

[[surface]]
id = "time-timespec-load"
c_symbols = ["timespec_load"]
rust_symbols = ["rs_timespec_load"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"
deviation_id = "time-timespec-load-null-fail-closed"
deviation_rationale = "The valid timespec conversion and infinity sentinel behavior follows C. Rust returns zero for malformed NULL input storage rather than relying on C's required-pointer contract."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-timespec-load"]

[[surface]]
id = "time-timespec-load-nsec"
c_symbols = ["timespec_load_nsec"]
rust_symbols = ["rs_timespec_load_nsec"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"
deviation_id = "time-timespec-load-nsec-null-fail-closed"
deviation_rationale = "The valid timespec nanosecond conversion and infinity sentinel behavior follows C. Rust returns zero for malformed NULL input storage rather than relying on C's required-pointer contract."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-timespec-load-nsec"]

[[surface]]
id = "time-timespec-store"
c_symbols = ["timespec_store"]
rust_symbols = ["rs_timespec_store"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "nullable"
runtime = "none"
verification = "static-only"
deviation_id = "time-timespec-store-null-fail-closed"
deviation_rationale = "The valid timespec sentinel and scalar conversion behavior follows C. Rust returns NULL for malformed NULL output storage rather than relying on C's required-pointer contract."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-timespec-store"]

[[surface]]
id = "time-timespec-store-nsec"
c_symbols = ["timespec_store_nsec"]
rust_symbols = ["rs_timespec_store_nsec"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "nullable"
runtime = "none"
verification = "static-only"
deviation_id = "time-timespec-store-nsec-null-fail-closed"
deviation_rationale = "The valid timespec sentinel and nanosecond conversion behavior follows C. Rust returns NULL for malformed NULL output storage rather than relying on C's required-pointer contract."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-timespec-store-nsec"]

[[surface]]
id = "time-timeval-load"
c_symbols = ["timeval_load"]
rust_symbols = ["rs_timeval_load"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "none"
runtime = "none"
verification = "static-only"
deviation_id = "time-timeval-load-null-fail-closed"
deviation_rationale = "The valid timeval conversion and infinity sentinel behavior follows C. Rust returns zero for malformed NULL input storage rather than relying on C's required-pointer contract."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-timeval-load"]

[[surface]]
id = "time-timeval-store"
c_symbols = ["timeval_store"]
rust_symbols = ["rs_timeval_store"]
coverage = "deviation"
abi = "c-exact"
bytes = "scalar"
errors = "nullable"
runtime = "none"
verification = "static-only"
deviation_id = "time-timeval-store-null-fail-closed"
deviation_rationale = "The valid timeval sentinel and microsecond conversion behavior follows C. Rust returns NULL for malformed NULL output storage rather than relying on C's required-pointer contract."
deviation_approval = "Reviewed static Rust-port boundary policy 2026-07-29."

[surface.static_fixture]
file = "tests-extra/test-time-util-rust.c"
meson_target = "test-time-util-rust"
labels = ["time-timeval-store"]

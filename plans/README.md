# Implementation Plans

Generated by the improve skill on 2026-07-31. Execute in the order below.
Each executor must read the complete plan, honor its STOP conditions, and
update the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Install the experimental Rust PID1 as an explicitly selected sidecar | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- Plan 001 is a prerequisite for reproducible image-based Rust PID1 boot
  validation. It deliberately does not make Rust the production default.

## Findings considered and rejected

- Replacing the installed `systemd` executable now: rejected because the Rust
  manager still lacks production private-bus, reexecution, shutdown, generator,
  serialization, watchdog, and security-startup parity.
- Installing the Rust binary unconditionally: rejected because ordinary
  developer and release installations must remain byte-for-byte unaffected
  unless the operator explicitly opts into the trial sidecar.
